#for installing dependencies:
npx expo install expo-camera expo-image-picker
npm install react-native-svg

#this is for tunnel hosting not local:
npx expo start --tunnel

#for local runs:
npx expo start 
